dependencies:
  override:
    - yarn
test:
  override:
    - yarn test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - ./publish release $CIRCLE_TAG
  master:
    branch: master
    commands:
      - ./publish dev
