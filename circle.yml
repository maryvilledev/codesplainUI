dependencies:
  override:
    - yarn
test:
  override:
    - yarn test:
        environment: #Don't expose our secrets while testing
          CLIENT_ID: birdperson
          CLIENT_SECRET: molting


deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - ./publish codesplain-ui
  master:
    branch: master
    commands:
      - ./publish codesplain-ui-dev
