dependencies:
  override:
    - yarn
test:
  override:
    - yarn test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - /bin/bash publishTag codesplain-ui
  master:
    branch: master
    commands:
      - /bin/bash publish codesplain-ui-dev
