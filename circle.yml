dependencies:
  override:
    - yarn
test:
  override:
    - yarn test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - ./publish codesplain-ui
  master:
    branch: [addS3deploy, master]
    commands:
      - ./publish codesplain-ui-dev
